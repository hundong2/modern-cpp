# 2026-08-06 이해 점검

## 1단계: C++ 문법

- [ ] `std::function<int(int)>`의 두 `int`가 각각 반환형과 매개변수형임을 설명한다.
- [ ] `pipeline`, 함수 반환 임시 객체, `std::move(output)`의 lvalue/prvalue/xvalue 구분을 말한다.
- [ ] `explicit`, 생성자에 반환형이 없는 이유, 멤버 초기화 목록의 역할을 코드 위치와 연결한다.
- [ ] 값 캡처, `const` 참조, `unique_ptr` 소유권과 각 객체의 수명 관계를 설명한다.

## 2단계: 아키텍처와 실행 관점

- [ ] 명령 파이프라인, 출력 포트, 콘솔 어댑터의 책임을 각각 한 문장으로 말한다.
- [ ] 가상 호출이 간접 호출일 수 있지만 컴파일러가 구체 형식을 알면 최적화할 수도 있음을 설명한다.
- [ ] 실제 로드·저장·분기·호출 명령은 CPU·ABI·컴파일러·옵션에 따라 달라진다고 말할 수 있다.

## 3단계: 오늘의 ICPC 문제

- [ ] BOJ 2252 **줄 세우기**의 방향 간선 `A -> B` 의미를 설명한다.
- [ ] [`../algorithm/topological-sort.md`](../algorithm/topological-sort.md)의 진입 차수 불변식을 말한다.
- [ ] Kahn 위상 정렬이 모든 선후 관계를 만족하는 이유를 설명한다.
- [ ] 시간 `O(N+M)`, 공간 `O(N+M)`을 정점·간선 처리 횟수와 연결한다.

## 4단계: 검증

- [ ] `daily_main`이 11, `daily_problem`이 24를 출력하고 성공 종료하는지 확인한다.
- [ ] CTest의 공식 예제형·사슬·독립 정점 입력이 모두 통과하는지 확인한다.
- [ ] 직접 작은 DAG를 그리고 매 단계의 큐, 진입 차수, 출력 순서를 손으로 추적한다.
- [ ] 순환 간선을 일부러 넣었을 때 결과 길이가 N보다 작아지는 이유를 설명한다.
