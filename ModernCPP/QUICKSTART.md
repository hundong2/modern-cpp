# 빠른 시작 가이드

이 가이드는 ModernCPP 프로젝트를 5분 안에 시작할 수 있도록 도와줍니다.

## 1단계: 환경 확인 (30초)

```bash
# 터미널을 열고 다음 명령어 실행
g++ --version
```

**출력 예시:**
```
g++ (Homebrew GCC 13.2.0) 13.2.0
```

✅ 버전이 10 이상이면 OK!
❌ 명령어를 찾을 수 없거나 버전이 낮으면 [환경 설정](README.md#환경-설정) 참조

## 2단계: Boost 확인 (30초)

```bash
brew list boost || echo "Boost 설치 필요"
```

❌ "Boost 설치 필요"가 나오면:
```bash
brew install boost
```

## 3단계: 프로젝트로 이동 (10초)

```bash
cd /path/to/modern-cpp/ModernCPP
```

## 4단계: 첫 빌드 (1-2분)

```bash
make
```

**출력 예시:**
```
📦 컴파일 중: src/server/main.cpp
📦 컴파일 중: src/client/main.cpp
🔨 서버 빌드 중...
✅ 서버 빌드 완료: ./build/server
🔨 클라이언트 빌드 중...
✅ 클라이언트 빌드 완료: ./build/client
🔨 예제 빌드 중: 01_concepts
✅ 예제 빌드 완료: ./build/examples/01_concepts
...
```

## 5단계: 첫 실행 (30초)

### 예제 실행해보기

```bash
./build/examples/01_concepts
```

**성공!** C++20 Concepts 예제가 실행됩니다.

### 서버-클라이언트 실행해보기

**터미널 1:**
```bash
make run-server
```

**터미널 2** (새 터미널 열기):
```bash
make run-client
```

**성공!** 서버와 클라이언트가 통신합니다.

## 문제 발생 시

### "boost/asio.hpp: No such file"

```bash
brew install boost
```

### "Address already in use"

```bash
lsof -i :8080
# 표시된 PID를 확인하고
kill -9 <PID>
```

### 컴파일 에러

```bash
# 처음부터 다시
make clean
make
```

## 다음 단계

✅ [전체 README.md](README.md) 읽기
✅ [학습 가이드](README.md#학습-가이드) 따라하기
✅ 예제 코드 수정해보기
✅ 나만의 기능 추가하기

## 명령어 치트시트

```bash
make              # 모든 것 빌드
make help         # 도움말
make clean        # 빌드 파일 삭제
make server       # 서버만 빌드
make client       # 클라이언트만 빌드
make examples     # 예제들만 빌드
make run-server   # 서버 실행
make run-client   # 클라이언트 실행
```

---

**준비 완료! 이제 C++20을 마스터할 시간입니다! 🚀**
