# 프로세스와 스레드

프로세스는 운영체제가 격리하는 자원/주소 공간 단위다. 스레드는 한 프로세스 안에서 코드·힙·파일 디스크립터를 공유하지만 각자 레지스터와 호출 스택을 가진 실행 흐름이다.

스레드 전환은 레지스터와 스케줄링 상태를 바꾸고 캐시 지역성을 해칠 수 있다. thread-per-client는 이해하기 쉽지만 연결 수가 커지면 스택 메모리와 스케줄링 비용이 증가한다. 정확한 기본 스택 크기는 OS/설정에 따라 다르므로 고정 숫자로 가정하지 않는다.
